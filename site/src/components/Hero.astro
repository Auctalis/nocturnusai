---
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<section class="relative min-h-screen flex flex-col justify-center items-center pt-20 overflow-hidden">
  <!-- Dynamic Background Orbs -->
  <div class="absolute inset-0 pointer-events-none">
    <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-emerald-600/20 rounded-full blur-3xl animate-pulse"></div>
    <div class="absolute bottom-1/4 right-1/4 w-64 h-64 bg-cyan-600/20 rounded-full blur-3xl animate-pulse delay-1000"></div>
  </div>

  <div class="container max-w-6xl mx-auto px-6 relative z-10 text-center">

    <!-- Pain Point Badge -->
    <div class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-red-500/10 border border-red-500/20 text-red-400 text-sm font-medium mb-8 animate-fade-in-up">
      <span class="relative flex h-2 w-2">
        <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75"></span>
        <span class="relative inline-flex rounded-full h-2 w-2 bg-red-500"></span>
      </span>
      Your agent already has the data. Your prompt is still too big.
    </div>

    <!-- Hero Title -->
    <h1 class="text-5xl md:text-7xl lg:text-8xl font-black tracking-tight mb-8 animate-fade-in-up delay-100">
      Large Turn Arrays In.<br />
      <span class="bg-clip-text text-transparent bg-gradient-to-r from-emerald-400 via-cyan-400 to-emerald-400 animate-gradient-x bg-300% pb-2">
        Lean Context Windows Out.
      </span>
    </h1>

    <!-- Positioning Statement -->
    <p class="text-xl md:text-2xl font-semibold text-slate-200 max-w-4xl mx-auto mb-4 animate-fade-in-up delay-200">
      Chat turns, tool output, CRM notes, internal comments, retries, stale summaries. The model sees all of it unless you cut it down first.
    </p>
    <p class="text-base md:text-lg text-slate-400 max-w-4xl mx-auto mb-4 animate-fade-in-up delay-200 leading-relaxed">
      NocturnusAI sits between your raw thread state and your next model call. Send the messy turn array in. Get a smaller, structured context window out. Use diffs so later turns only pay for what changed.
    </p>
    <p class="text-base md:text-lg text-slate-500 max-w-3xl mx-auto mb-12 animate-fade-in-up delay-200">
      The context reduction layer for production AI agents.
    </p>

    <!-- Framework compatibility — above the fold -->
    <div class="flex flex-wrap items-center justify-center gap-3 mb-8 animate-fade-in-up delay-300">
      <span class="text-xs text-slate-600 uppercase tracking-wider font-medium">Works with</span>
      {[
        { name: "LangChain", href: `${base}/docs/integrations#langchain` },
        { name: "CrewAI", href: `${base}/docs/integrations#crewai` },
        { name: "AutoGen", href: `${base}/docs/integrations#autogen` },
        { name: "OpenAI Agents", href: `${base}/docs/integrations#openai-agents` },
        { name: "Anthropic", href: `${base}/docs/integrations#anthropic` },
        { name: "Claude", href: `${base}/docs/mcp` },
        { name: "Cursor", href: `${base}/docs/mcp` },
        { name: "Any MCP client", href: `${base}/docs/mcp` },
      ].map(f => (
        <a href={f.href} class="px-3 py-1 rounded-full bg-white/5 border border-white/10 text-xs font-medium text-slate-400 hover:text-emerald-400 hover:border-emerald-400/30 transition-colors">{f.name}</a>
      ))}
    </div>

    <!-- Install Command — Full Width -->
    <div class="w-full mx-auto mb-2 animate-fade-in-up delay-300">
      <div class="flex items-center gap-4 px-6 py-4 rounded-xl bg-white/5 border border-white/10 backdrop-blur-sm font-mono text-sm md:text-base whitespace-nowrap overflow-x-auto">
        <span class="text-emerald-400 shrink-0">$</span>
        <span class="text-slate-200">curl -fsSL https://raw.githubusercontent.com/Auctalis/nocturnusai/main/install.sh | bash</span>
      </div>
      <p class="text-xs text-slate-500 text-center mt-2">Requires <a href="https://docs.docker.com/get-docker/" class="text-slate-400 underline underline-offset-2 hover:text-white transition-colors">Docker</a> with Compose V2 installed. Or <a href={`${base}/docs#install`} class="text-emerald-500/70 underline underline-offset-2 hover:text-emerald-400 transition-colors">install with an AI prompt</a>.</p>
    </div>

    <!-- CTAs -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-4 mb-16 animate-fade-in-up delay-300">
      <a href={`${base}/docs`} class="px-8 py-4 rounded-full bg-gradient-to-r from-emerald-600 to-cyan-600 text-white font-bold hover:shadow-[0_0_30px_rgba(16,185,129,0.5)] transition-all hover:-translate-y-1 transform">
        See The Workflow ->
      </a>
      <a href={`${base}/docs/concepts`} class="px-8 py-4 rounded-full bg-white/5 border border-white/10 text-slate-300 font-medium hover:bg-white/10 hover:border-white/20 transition-all hover:-translate-y-1 transform">
        Backend Mechanics
      </a>
    </div>

    <!-- Stack Summary Pills -->
    <div class="flex flex-wrap items-center justify-center gap-2 mb-12 animate-fade-in-up delay-300">
      {[
        "Turn Reduction First",
        "Goal-Specific Context Windows",
        "Incremental Context Diffs",
        "Contradiction Checks",
        "Memory Lifecycle",
        "MCP · SDK · REST",
        "Backend Mechanics In The Back",
      ].map(label => (
        <span class="px-3 py-1 rounded-full bg-white/5 border border-white/8 text-xs font-mono text-slate-500 hover:text-slate-300 hover:border-white/20 transition-colors cursor-default">
          {label}
        </span>
      ))}
    </div>

    <!-- Terminal Window — Simple API Demo -->
    <div class="w-full max-w-3xl mx-auto rounded-xl overflow-hidden bg-[#0f172a]/90 backdrop-blur-xl border border-white/10 shadow-2xl animate-fade-in-up delay-400 group hover:border-emerald-500/30 transition-colors">
      <!-- Terminal Header -->
      <div class="flex items-center gap-2 px-4 py-3 bg-white/5 border-b border-white/5">
        <div class="w-3 h-3 rounded-full bg-red-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-yellow-500/80"></div>
        <div class="w-3 h-3 rounded-full bg-green-500/80"></div>
        <div class="ml-4 text-xs font-mono text-slate-500">messy thread in, usable window out</div>
      </div>

      <!-- Terminal Body -->
      <div class="p-6 text-left font-mono text-sm space-y-3">
        <div class="text-slate-500">
          # 1. Send the raw thread state
        </div>
        <div class="flex items-start gap-2 text-slate-300">
          <span class="text-emerald-500 shrink-0">$</span>
          <span>POST /context &nbsp;&#123; "turns": ["user msg", "crm lookup", "tool log", "internal note", "..."] &#125;</span>
        </div>
        <div class="text-emerald-400 text-sm">
          ✓ 8 context entries selected from a much larger thread
        </div>

        <div class="text-slate-500 mt-2">
          # 2. Feed only the reduced window to the model
        </div>
        <div class="flex items-start gap-2 text-slate-300">
          <span class="text-emerald-500 shrink-0">$</span>
          <span>openai.chat(system=context_window, user="Write the next support reply")</span>
        </div>
        <div class="text-emerald-400 text-sm">
          ✓ Same thread state, less noise, lower token cost
        </div>
        <div class="text-emerald-400/60 text-xs pl-4">
          keep sessionId -> later turns can use /context/diff instead of replaying everything
        </div>

        <div class="mt-4 pt-3 border-t border-white/5">
          <div class="flex items-center justify-between text-xs">
            <span class="text-slate-500">What changes in practice:</span>
            <span><span class="text-red-400 line-through">full thread every turn</span> <span class="text-slate-600">-></span> <span class="text-emerald-400 font-bold">reduced window + diffs</span></span>
          </div>
        </div>
      </div>
    </div>

  </div>
</section>

<style>
  .bg-300\% {
    background-size: 300% 300%;
  }
  @keyframes gradient-x {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  .animate-gradient-x {
    animation: gradient-x 15s ease infinite;
  }

  @keyframes fade-in-up {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-in-up {
      animation: fade-in-up 0.8s ease-out forwards;
      opacity: 0;
  }
  .delay-100 { animation-delay: 0.1s; }
  .delay-200 { animation-delay: 0.2s; }
  .delay-300 { animation-delay: 0.3s; }
  .delay-400 { animation-delay: 0.4s; }
  .delay-1000 { animation-delay: 1s; }

  .border-white\/8 {
    border-color: rgba(255, 255, 255, 0.08);
  }
</style>
